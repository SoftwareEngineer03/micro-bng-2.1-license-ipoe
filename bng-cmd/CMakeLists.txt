SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -Wextra -D_GNU_SOURCE")

ADD_DEFINITIONS(-DMICRO_BNG_VERSION="${MICRO_BNG_VERSION}")

ADD_EXECUTABLE(bng-cmd
	bng_cmd.c
)

INSTALL(TARGETS bng-cmd
	RUNTIME DESTINATION bin
)
INSTALL(FILES bng-cmd.1
	DESTINATION share/man/man1
)
